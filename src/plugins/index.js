import Vue from 'vue'
import {
  NavBar,
  Icon,
  Tabbar, TabbarItem,
  Tab, Tabs,
  Search
} from 'vant'

Vue
  .use(NavBar)
  .use(Icon)
  .use(Tabbar).use(TabbarItem)
  .use(Tab).use(Tabs)
  .use(Search)
